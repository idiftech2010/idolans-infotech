kind = "web"
previewPath = "/"
title = "Idolans Info-Tech"
version = "1.0.0"
id = "artifacts/idolans"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20592

[services.development]
run = "pnpm --filter @workspace/idolans run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/idolans", "run", "build" ]
publicDir = "artifacts/idolans/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20592"
BASE_PATH = "/"
